import { ArduinoRegistrationGuard } from './arduino-registration.guard'

describe('ArduinoRegistrationGuard', () => {
  it('should be defined', () => {
    expect(new ArduinoRegistrationGuard()).toBeDefined()
  })
})
